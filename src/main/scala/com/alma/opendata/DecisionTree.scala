package com.alma.opendata

import org.apache.spark.rdd.RDD
import org.apache.spark.{SparkConf, SparkContext}

/**
  * Decision tree used to analyze aggregate entries from Web Data Commons
  */
object DecisionTree {


	/**
	 * Run the decision tree and indeitfy n-quads related to Nantes
	 * @param nquads
	 * @return
	 */
	// def run(nquads: RDD[(String, String)]) : RDD[String] = {
	// 	nquads
	// }
}
